seed: 6304

data:
  root: ./data_raw
  n_classes: 10

training:
  lr: 0.1
  weight_decay: 5.0e-4
  batch_size: 128
  max_epochs: 100

method:
  name: vanilla

checkpoint_path: task4/results/checkpoints/vanilla.pt
